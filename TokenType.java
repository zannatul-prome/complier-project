public enum TokenType {
    NUMBER,     // সংখ্যা
    PLUS,       // যোগ
    MINUS,      // বিয়োগ
    MULTIPLY,   // গুণ
    ASSIGN,     // রাখ
    IDENTIFIER, // নাম
    SEMICOLON,  // ;
    IF,         // যদি
    ELSE,       // নাহলে
    THEN,       // তাহলে
    GT,         // >
    LT,         // <
    EQ,         // ==
    LEFT_PAREN, // (
    RIGHT_PAREN,// )
    EOF;
}