public enum TokenType {
    NUMBER,
    IDENTIFIER,
    ASSIGN,
    PLUS,
    MINUS,
    MULTIPLY,
    GT,
    LT,
    LEFT_PAREN,
    RIGHT_PAREN,
    SEMICOLON,
    IF,
    ELSE,
    THEN,
    EOF
}